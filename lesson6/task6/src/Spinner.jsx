import React from 'react';

const Spinner = (props) => {
    return (<span style= {{width: `${props.size}px`, height: `${props.size}px`}} className="spinner" >Login</span>);
}

export default Spinner;

        